target 'WonderPush' do
    platform :ios, '7.0'

    pod 'WonderPush', :path => '.'
end

target 'NotificationServiceExtension' do
    platform :ios, '7.0'

    pod 'WonderPush/NotificationServiceExtension', :path => '.'
end
